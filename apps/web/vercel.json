{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "cd ../.. && pnpm install --frozen-lockfile --filter @adv/web... --filter advertising",
  "buildCommand": "cd ../.. && pnpm --filter @adv/web... run build && sh -c 'if [ -n \"$DATABASE_URL\" ]; then pnpm db:migrate; else echo \"DATABASE_URL not set - skipping pnpm db:migrate\"; fi'",
  "outputDirectory": ".next"
}
